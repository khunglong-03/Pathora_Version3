// Canonical StatCard moved to @/components/ui/StatCard
// This barrel export maintains backward compatibility for existing imports
export { default, StatCard as StatCard, type StatCardProps } from "@/components/ui/StatCard";
